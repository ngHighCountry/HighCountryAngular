import { ElementRef, AfterViewInit } from '@angular/core';
import { NgxMdService } from './ngx-md.service';
export declare class NgxMdComponent implements AfterViewInit {
    private _mdService;
    private _el;
    private _platformId;
    private _path;
    private _data;
    private _md;
    private _ext;
    constructor(_mdService: NgxMdService, _el: ElementRef, _platformId: string);
    path: string;
    data: string;
    ngAfterViewInit(): void;
    onDataChange(data: string): void;
    /**
     * get remote conent;
     */
    onPathChange(): void;
    /**
     * catch http error
     */
    private handleError(error);
    processRaw(data: any): void;
    /**
     * Prepare string
     */
    prepare(raw: string): string;
    /**
     * Trim left whitespace
     */
    private trimLeft(line);
    /**
     * Use Prism to highlight code snippets only on the browser
     */
    private highlightContent(async);
}
